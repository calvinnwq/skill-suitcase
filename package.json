{
  "name": "skill-suitcase",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only skill install planner for portable agent skill catalogs.",
  "type": "module",
  "bin": {
    "suitcase": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "typecheck": "node ./scripts/check-js-syntax.mjs src test",
    "lint": "node ./scripts/check-js-syntax.mjs src test",
    "build": "node ./scripts/check-js-syntax.mjs src test",
    "format:check": "node ./scripts/check-js-syntax.mjs src test"
  },
  "engines": {
    "node": ">=20"
  }
}
